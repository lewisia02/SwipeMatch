# プロジェクトメモリ (CLAUDE.md)

このファイルは、AIアシスタント（Claude Code等）と人間が協働するための「スペック駆動開発」の基本ルールとプロジェクトのコンテキストを定義するドキュメントです。

## 1. プロジェクトの前提条件・ツール
- 本プロジェクトでは、AI CLIツール（Claude Code等）とカスタムコマンド・スキルを活用して開発を進めます。
- `/setup-project`、`/add-feature` などのコマンドや、`Skill('steering')` はプロジェクト固有の環境に組み込まれた拡張機能として動作することを前提としています。

## 2. 技術スタック

- **開発環境**: devcontainer（`.devcontainer/` で定義。Node.js を同梱）
- **フロントエンド**: Next.js (App Router), TypeScript 5.x, Tailwind CSS
- **バックエンド/ストレージ**: Supabase（Database & Storage、無料枠で運用）
- **スワイプUI実装**: `react-tinder-card` または `framer-motion`
- **ランタイム**: Node.js 24.x
- **パッケージ／環境管理**: npm
- **テスト**: Vitest（ユニット/統合）、Playwright（E2E）
- **Lint/Format**: ESLint、Prettier

詳細は `docs/architecture.md`・`docs/development-guidelines.md` を参照。

## 3. スペック駆動開発の基本原則

### 3.1 基本フロー

1. **ドキュメント作成**: 永続ドキュメント(`docs/`)で「何を作るか」を定義
2. **作業計画**: ステアリングファイル(`.steering/`)で「今回何をするか」を計画
3. **実装**: tasklist.mdに従って実装し、進捗を随時更新
4. **検証**: テストと動作確認
5. **更新**: 必要に応じてドキュメント更新

### 3.2 重要なルール

#### ドキュメント作成時

**1ファイルずつ作成し、必ずユーザーの承認を得てから次に進む**

承認待ちの際は、明確に伝える:

> 「[ドキュメント名]の作成が完了しました。内容を確認してください。承認いただけたら次のドキュメントに進みます。」

#### 実装前の確認

新しい実装を始める前に、必ず以下を確認:

1. このファイル（CLAUDE.md）を読む
2. 関連する永続ドキュメント(`docs/`)を読む
3. Grepで既存の類似実装を検索
4. 既存パターンを理解してから実装開始

## 4. ステアリングファイルとスキルの管理

作業単位の計画・実装・検証は `.steering/` ディレクトリと `steering` スキルを用いて一元管理します。

### 4.1 ディレクトリ・ファイル構成

作業ごとに `.steering/[YYYYMMDD]-[タスク名]/` （例: `20260714-add-user-profile`）を作成し、以下のファイルを配置します。

- `requirements.md`: 今回の要求内容
- `design.md`: 実装アプローチ
- `tasklist.md`: 具体的なタスクリスト

### 4.2 スキルの使用方法

作業計画・実装・検証時は必ず `steering` スキルを使用してください。
- **作業計画時**: `Skill('steering')`でモード1（ステアリングファイル作成）
- **実装時**: `Skill('steering')`でモード2（実装とtasklist.md更新管理）
- **検証時**: `Skill('steering')`でモード3（振り返り）
※ 詳細な手順と更新管理のルールはsteeringスキル内に定義されています。

### 4.3 ドキュメント横断整合性チェック

`docs/`配下の7つの永続ドキュメントとCLAUDE.mdは互いに用語・数値・仕様を参照し合っており、1つを編集しても他への波及は自動検出されません。この波及を検出するため `consistency-check` スキルを用意しています。

- **起動方法**: `docs/**/*.md` または `CLAUDE.md` をEdit/Writeすると、PostToolUseフック（`.claude/settings.json`）が「横断チェックを検討してください」というリマインドをコンテキストに差し込みます（強制ではなく気づき用）。手動では `/check-consistency` で実行できます
- **チェック方式**: `.claude/skills/consistency-check/concept-map.md` に維持している概念マップを起点に、変更内容に関連する概念が登場する他ドキュメントだけを絞り込んで確認します（低コスト）。網羅的な全ドキュメント総当たりは高コストなため、`--full`指定時のみユーザー確認の上で実行します
- 新しい横断的概念を見つけたら、`concept-map.md` に追記してマップを育てていきます

## 5. ディレクトリ構造（永続的ドキュメント）

アプリケーション全体の「何を作るか」「どう作るか」を定義する `docs/` ディレクトリの構成です。

### 下書き・アイデア（`docs/ideas/`）

- 壁打ち・ブレインストーミングの成果物 / 技術調査メモ
- 自由形式（構造化は最小限）
- `/setup-project`実行時に自動的に読み込まれる

### 正式版ドキュメント

各ドキュメントは対応するスキルを用いて生成・編集します。

- **product-requirements.md** - プロダクト要求定義書（`prd-writing` スキル）
- **functional-design.md** - 機能設計書（`functional-design` スキル）
- **ui-design.md** - 画面設計書（`ui-design` スキル。Web/GUI の場合）
- **architecture.md** - 技術仕様書（`architecture-design` スキル）
- **repository-structure.md** - リポジトリ構造定義書（`repository-structure` スキル）
- **development-guidelines.md** - 開発ガイドライン（`development-guidelines` スキル）
- **glossary.md** - ユビキタス言語定義（`glossary-creation` スキル）

### ドキュメント管理の原則

- **永続的ドキュメント（`docs/`）**: 基本設計を記述し、頻繁には更新しない。プロジェクト全体の「北極星」として機能する。
- **作業単位のドキュメント（`.steering/`）**: 特定の作業に特化し、作業ごとに新規作成する。各開発者のローカル環境上で作業履歴として保持し、Gitのコミット対象には含めない（`.gitignore` 対象。詳細は `docs/repository-structure.md` の「除外設定」、Git運用ルールは `docs/development-guidelines.md` を参照）。リポジトリ上に恒久的な記録として残したい設計判断や振り返りの要点は、完了後に `docs/` 配下の該当ドキュメントへ転記する。

## 6. 開発プロセス

### 初回セットアップ
1. このテンプレートを使用
2. `/setup-project` で永続的ドキュメント作成(対話的に7つ作成)
3. `/add-feature [機能]` で機能実装

### 日常的な使い方

**基本は普通に会話で依頼してください:**

```text
# ドキュメントの編集
> PRDに新機能を追加してください
> architecture.mdのパフォーマンス要件を見直して
> glossary.mdに新しいドメイン用語を追加

# 機能追加(定型フローはコマンド)
> /add-feature ユーザープロフィール編集

# 次に何をすべきか迷ったとき
> /next

# 詳細レビュー(詳細なレポートが必要なとき)
> /review-docs docs/product-requirements.md

# 複数ドキュメントにまたがる仕様変更をした後の横断チェック
> /check-consistency
```
**ポイント**: スペック駆動開発の詳細を意識する必要はありません。AIが適切なスキルを判断してロードします。

## 7. CLAUDE.md 自体の更新ルール
このファイル（CLAUDE.md）はプロジェクトの基本ルールを定義する重要なファイルです。以下の条件に当てはまる場合は、本ファイルを更新してください。
- **技術スタックの変更時**（Node.jsのバージョンアップ、新ツールの導入など）
- **カスタムコマンドやスキルの追加・変更時**
- **開発プロセスやドキュメントルールの改善時**
※ 更新する際は、必ずユーザー（人間）の承認を得た上で追記・修正を行ってください。
